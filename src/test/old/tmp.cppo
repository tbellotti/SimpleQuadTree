#include "../include/refinealwayscriterion.h"
#include <iostream>
#include <memory>
#include "../include/cell.h"

int main ()
{

    RefineAlwaysCriterion<double> crit;


    auto pt = std::shared_ptr<Cell<double>>(new Cell<double>(Point<double>(1.0, 1.0), 10.0, 50.0, 1));

    std::cout<<crit(pt)<<std::endl;    

    return 0;
}
